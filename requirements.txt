# =============================================================================
# spec-reviewer — зависимости проекта
# Только то, что реально импортируется кодом app/ и modules/.
# Из поставки langchain_module нужен лишь langchain-core (интерфейсы BaseChatModel);
# langchain / langchain-openai / openai / langchain-community / faiss-cpu удалены:
# код, который их использовал, из проекта вычистен (RAG и сторонние бэкенды не нужны
# по ТЗ).
# =============================================================================
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
python-dotenv>=1.0
requests>=2.31

# langchain_module — интерфейс чат-модели для моста bridges/genapi.py
langchain-core>=0.3

# Тесты (вне проекта — HWV/tests/run_api_tests.py; скрипт работает через fastapi.testclient)
httpx>=0.27